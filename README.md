Npm init -y to initialize next project
install the express npn i express
install ts:- npm install -D typescript @types/node @types/express tsx

install zod and dotenv to config env:- npm install zod dotenv

install jest for testing:-  npm install -D jest ts-jest @types/jest supertest @types/supertest

npm test — jest in watch mode
npm run test:run — single test run

understand the concept of status codes (HTTP protocol)

install postgresql:- sudo apt install postgresql postgresql-contrib
verify psql --version


`sudo -u postgres psql -c "CREATE DATABASE test_task_db;"
sudo -u postgres psql -c "ALTER USER postgres PASSWORD 'postgres';"`